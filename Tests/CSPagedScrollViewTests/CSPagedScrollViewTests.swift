import XCTest
@testable import CSPagedScrollView

final class CSPagedScrollViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CSPagedScrollView().text, "Hello, World!")
    }
}
